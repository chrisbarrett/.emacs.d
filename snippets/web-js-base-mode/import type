# -*- mode: snippet -*-
# name: import type
# key: it
# condition: (and (yas-funcs-bolp) (not (save-excursion (search-backward-regexp (rx bol (* space) symbol-start "describe" symbol-end) nil t))))
# --
import type { ${1:T} } from '$0';