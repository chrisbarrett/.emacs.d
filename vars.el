;;; vars.el --- Shared configuration variables.  -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(defvar vars-default-theme 'doom-one
  "The theme to use when Emacs first starts up.")

(defvar vars-default-text-height 100
  "The default text height. Bump this value for HIDPI screens.")

(defvar vars-default-font-family "Iosevka"
  "The font family to use for the default face.")

(provide 'vars)

;;; vars.el ends here
