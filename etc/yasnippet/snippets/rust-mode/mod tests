# key: test
# name: mod tests
# condition: (and (yas-funcs-bolp) (not (yas-funcs-rs-in-test-module-p)))
# --
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_${1:feature}() {
        $0
    }
}