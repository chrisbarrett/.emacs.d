# key: up
# name: use-package
# expand-env: ((yas-indent-line (quote fixed)) (yas-wrap-around-region (quote t)))
# condition: (yas-funcs-bolp)
# --
(use-package ${1:name}
  :config
  (progn
    $0
    ))
