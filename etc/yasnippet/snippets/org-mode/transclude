# -*- mode: snippet -*-
# name: transclude (incl heading)
# key: tt
# condition: (yas-funcs-bolp)
# type: command
# --
(let ((link (org-funcs-read-roam-node-link)))
  (org-insert-heading)
  (insert (concat link "\n" "#+transclude: " link " :only-contents"))
  (org-transclusion-add))