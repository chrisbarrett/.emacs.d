# key: it
# name: it
# condition: (and (yas-funcs-bolp) (save-excursion (search-backward-regexp (rx bol (* space) symbol-start "describe" symbol-end) nil t)))
# --
it('${1:behaviour}', () => {
  $0
});